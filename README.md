# Vibe-Coding-a-GUI-App

## Rock Paper Scissors

Run the game with:

```bash
python rock_paper_scissors.py
```

Type `rock`, `paper`, `scissors` (or `r`, `p`, `s`) to play each round.
Type `quit` to end the game.
